# Dev & CI tooling — pinned so local checks match CI exactly.
# Install:  pip install -r requirements.txt -r requirements-dev.txt
#
# (CI previously did `pip install mypy` / `pip install ruff` unpinned and without
# type stubs, so CI checked something different from any local run — pin it all.)

ruff==0.15.0
mypy==1.14.1
types-requests
types-PyYAML
bandit==1.9.3
detect-secrets==1.5.0
pytest>=7.4.0
pre-commit>=3.6.0
